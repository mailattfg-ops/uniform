"  return (\n    <div className=\"space-y-8 animate-in fade-in duration-500\">\n\n      {/* Header */}\n      <div className=\"border-b border-zinc-100 pb-6 flex justify-between items-center flex-wrap gap-4\">\n        <div>\n          <h3 className=\"text-2xl font-black italic text-[#3a525d]\">\n            {quotationType === 'SET_TYPE' ? 'Custom Sets Builder' : 'Sizing & Measurement Audits'}\n          </h3>\n          <p className=\"text-[10px] font-black uppercase tracking-widest text-[#2d8d9b] mt-1 opacity-70\">\n            {quotationType === 'SET_TYPE' ? 'Configure custom sets and pricing' : 'Live verification of measurements profiles'}\n          </p>\n        </div>\n        <div className=\"flex items-center gap-3\">\n          <span className=\"px-3 py-1.5 rounded-xl text-[10px] font-black uppercase bg-[#2d8d9b]/5 text-[#2d8d9b] border border-[#2d8d9b]/10\">\n            {quotationType === 'SET_TYPE'\n              ? `${manualItems.reduce((acc, it) => acc + (parseInt(it.quantity) || 0), 0)} Sets`\n              : hasDepartments\n                ? `${Object.values(departmentItems).reduce((acc, items) => acc + items.reduce((sum, it) => sum + (parseInt(it.quantity) || 0), 0), 0)} Units`\n                : hasMeasurements\n                  ? `${orgAnalysis.total_entities} Entities`\n                  : `${manualItems.reduce((acc, it) => acc + (parseInt(it.quantity) || 0), 0)} Custom Units`}\n          </span>\n        </div>\n      </div>\n\n      {quotationType === 'SET_TYPE' ? (\n        <div className=\"space-y-6\">\n          <div className=\"flex items-center justify-between flex-wrap gap-4\">\n            <div className=\"flex items-center gap-2\">\n              <Package size={18} className=\"text-[#2d8d9b]\" />\n              <h4 className=\"text-[10px] font-black uppercase tracking-widest text-[#3a525d]\">\n                Configure Uniform Sets & Complete Pricing\n              </h4>\n            </div>\n            <button\n              onClick={() => {\n                const newSet: ManualIte
<truncated 14954 bytes>